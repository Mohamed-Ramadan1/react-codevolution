import React from 'react';

const Hero = ({ heroName }) => {
    // if (heroName ==="poor") throw new Error("you can do it ");
    return (
        <dvi>
            {heroName}
        </dvi>
    )
}
export default Hero;